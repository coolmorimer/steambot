apiVersion: v1
kind: Secret
metadata:
  name: steambot-secrets
  namespace: steambot
type: Opaque
stringData:
  # Заменить на реальные значения перед деплоем!
  # kubectl create secret generic steambot-secrets \
  #   --from-literal=DB_PASSWORD='...' \
  #   --from-literal=JWT_SECRET='...' \
  #   -n steambot
  DB_PASSWORD: "CHANGE_ME_STRONG_PASSWORD"
  JWT_SECRET: "CHANGE_ME_RANDOM_64_CHARS"
  STRIPE_SECRET_KEY: ""
  STRIPE_WEBHOOK_SECRET: ""
  SMTP_HOST: ""
  SMTP_USER: ""
  SMTP_PASS: ""
